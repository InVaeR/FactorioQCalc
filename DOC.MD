# DOC.MD — Factorio Quality Calculator

## 1. Структура проекта

```
FactorioQCalc/
├── main.py                 # Точка входа, запуск QApplication
├── core/
│   ├── __init__.py
│   ├── data.py             # Таблицы бонусов, названия качеств, MAX_MODULE_SLOTS
│   └── calculator.py       # Логика расчёта + total_chance_from_modules
├── ui/
│   ├── __init__.py
│   ├── assets.py           # QUALITY_ICONS, MODULE_ICONS, пути
│   ├── style.py            # Палитра + build_stylesheet()
│   ├── main_window.py      # QMainWindow, двухпанельная компоновка
│   └── widgets/
│       ├── __init__.py
│       ├── panel.py        # FactorioPanel (выпуклая панель с заголовком)
│       ├── module_slot.py  # ModuleSlot — квадратный слот с иконкой+бейджем
│       ├── module_row.py   # ModuleRow — ряд слотов + редактор + Σ
│       └── quality_bar.py  # QualityBar — прогресс-бар качества
├── tests/
│   ├── __init__.py
│   └── test_calculator.py  # Юнит-тесты (45 тестов)
├── assets/
│   └── images/
│       ├── Quality_normal.png       # Иконка Normal (64×64)
│       ├── Quality_uncommon.png     # Иконка Uncommon (64×64)
│       ├── Quality_rare.png         # Иконка Rare (64×64)
│       ├── Quality_epic.png         # Иконка Epic (64×64)
│       ├── Quality_legendary.png    # Иконка Legendary (64×64)
│       ├── Quality_module.png       # Иконка Quality Module 1 (64×64)
│       ├── Quality_module_2.png     # Иконка Quality Module 2 (64×64)
│       ├── Quality_module_3.png     # Иконка Quality Module 3 (64×64)
│       ├── chevron-arrow-up_14.png  # Стрелка вверх (14×14)
│       └── chevron-arrow-down_14.png# Стрелка вниз (14×14)
├── pyproject.toml          # Конфигурация и зависимости
├── requirements-dev.txt    # Dev-зависимости (pytest)
├── SPEC.MD                 # Спецификация
└── DOC.MD                  # Документация (текущий файл)
```

## 2. Архитектура

```
core/calculator.py  ←→  core/data.py         # Чистая логика, без Qt
        ↕
ui/main_window.py   ←→  ui/widgets/*         # UI + кастомные виджеты
        ↕
main.py             ←→  QApplication         # Точка входа
```

- **`core/`** не импортирует PySide6 и может быть покрыт тестами
- **`ui/`** использует Qt для отображения и вызывает функции `core/`
- **`widgets/`** содержит переиспользуемые кастомные виджеты (ModuleSlot, ModuleRow, QualityBar)
- **`assets/`** содержит иконки и графику для Factorio-стиля

## 3. Ядро расчёта

### `core/data.py`
- `QUALITY_NAMES` — английские названия уровней (1–5)
- `QUALITY_NAMES_RU` — русские названия
- `TIER_NAMES` — названия модулей
- `BONUS_TABLE` — таблица бонусов `[tier][quality]`
- `MAX_QUALITY_LEVEL = 5`
- `MAX_MODULE_SLOTS = 4` — число слотов модулей

### `core/calculator.py`

**`quality_bonus(tier, quality) → float`**
- Возвращает бонус одного модуля (0–1)
- `tier`: 1, 2, 3
- `quality`: 1 (Normal) … 5 (Legendary)
- Исключение при невалидных параметрах

**`total_chance(count, tier, quality) → float`**
- Суммарный шанс качества = `count × bonus`, ограничен 1.0

**`total_chance_from_modules(modules: list[(tier, quality) | None]) → float`**
- Суммарный шанс из списка слотов (каждый = `(tier, quality)` или `None`)
- Ограничен 1.0, проверяет `len(modules) ≤ MAX_MODULE_SLOTS`
- Не ломает обратную совместимость `total_chance`

**`distribute(start_level, chance, output_per_min, max_unlocked_level) → dict`**
- Возвращает `{level: (share, items_per_min)}`
- Каскадное улучшение: 90% на следующий уровень, 10% на каскад далее
- Потолок: `max_unlocked_level` (весь излишек накапливается на нём)
- Если `start_level == max_unlocked_level` → 100% остаётся на месте

#### Формула каскада

Для уровня `start_level = s` и `max_unlocked_level = M`:

| Уровень | Доля |
|---------|------|
| `s` | `1 − Q` |
| `s + k` (для `k < M−s`) | `Q × 0.9 × 0.1^{k−1}` |
| `M` | `Q × 0.1^{M−s−1}` |

Где `Q` — суммарный шанс качества.

## 4. UI — MainWindow

### Компоновка (двухпанельная)

```
┌──────────────────────────────────────────────────────────────┐
│  КАЛЬКУЛЯТОР КАЧЕСТВА FACTORIO   [иконка]                    │
├───────────────────────────────┬──────────────────────────────┤
│  ПРОИЗВОДСТВО                 │  СВОДКА                      │
│  Выработка [60шт/мин ▾]      │  [████████░░] Σ шанс: 24.80%  │
│  Нач. качество [◈ Normal ▾]  │  Макс: Legendary             │
│  Макс. качество [◈ Legend ▾] │  Коэфф. рецикл.: 0.1234      │
├───────────────────────────────┤                              │
│  МОДУЛИ КАЧЕСТВА (4 слота)    ├──────────────────────────────┤
│  [i1][i2][i3][i4]  Σ: X%     │  Таблица распределения       │
│  [tier ▾] [quality ▾] [×]    │  (иконка|доля|шт/мин)        │
├───────────────────────────────┤  ИТОГО / ПОТЕРИ / ВХОД      │
│  ☑ РЕЦИКЛИНГ                  ├──────────────────────────────┤
│  Порог [◈ Normal ▾]          │  [Копировать] [Сброс]        │
│  [i1][i2][i3][i4]  Σ: X%     │                              │
│  [tier ▾] [quality ▾] [×]    │                              │
├───────────────────────────────┤                              │
│      [ РАССЧИТАТЬ ]           │                              │
└───────────────────────────────┴──────────────────────────────┘
```

- Левая панель (фикс. ширина ~400px): ввод параметров
- Правая панель (растяжение): результаты + таблица
- Каждая секция — `FactorioPanel` с заголовком

### Используемые компоненты
- `QMainWindow` — главное окно
- `FactorioPanel(QFrame)` — кастомная панель с заголовком (2px bevel)
- `ModuleSlot(QFrame)` — квадратный слот модуля с paintEvent (иконка + бейдж качества)
- `ModuleRow(QWidget)` — ряд из 4 слотов + редактор + live Σ шанса
- `QualityBar(QWidget)` — прогресс-бар качества (зелёный/жёлтый)
- `QComboBox` — выбор качества, уровня модуля, единиц выработки, порога рециклинга
- `QDoubleSpinBox` — выработка (0–1 000 000)
- `QPushButton` — «РАССЧИТАТЬ» (primary), «Копировать» (success), «Сброс» (danger)
- `QTableWidget` — таблица распределения (3 столбца: качество, доля, шт/мин)
- `QMessageBox` — предупреждения валидации

### Property-based варианты кнопок
- `variant="primary"` — оранжевый (по умолчанию)
- `variant="success"` — зелёный (Копировать)
- `variant="danger"` — красный (Сброс/Очистить)

### Factorio-стиль (QSS)

#### Цветовая палитра

| Роль | Цвет |
|------|------|
| Фон приложения | `#1a1a1a` |
| Фон панелей | `#2b2b2b` |
| Заголовки панелей | `#3a3a3a` |
| Заголовки (текст) | `#ffcc00` (жёлтый) |
| Акцент (кнопки) | `#d97b14` (оранжевый) |
| Акцент (светлый) | `#f09030` |
| Акцент (тёмный) | `#8a4a00` |
| Зелёный (success) | `#4a8c1c` |
| Красный (danger) | `#cc3333` |
| Основной текст | `#e0e0e0` |
| Второстепенный текст | `#a0a0a0` |
| Bevel светлый | `#5a5a5a` |
| Bevel тёмный | `#0a0a0a` |
| Чередование строк | `#333333` |

#### Bevel-эффекты
- **Выпуклые** (панели): светлая верх/левая граница, тёмная низ/правая
- **Вдавленные** (поля ввода, слоты): тёмная верх/левая граница, светлая низ/правая
- **Кнопка pressed**: инверсия границ + смещение текста (padding)

#### Состояния интерактивности
- `:hover` — осветление фона на ~10%
- `:focus` — оранжевая рамка (`#d97b14`)
- `:pressed` — инверсия bevel-границ, затемнение фона
- `cursor: pointer` на кнопке

#### Иконки
- Качество: 28×28, отображаются в комбобоксах, таблице и бейджах слотов
- Модули: 28×28, в комбобоксах и слотах
- Стрелки спинбоксов и дропдаунов: 14×14, PNG из `assets/images/`

#### Скроллбар
- Трек `#1a1a1a`, ползунок `#3a3a3a` с bevel, `:hover` → `#4a4a4a`

## 5. Рециклинг

### UI
- `QCheckBox` «Включить рециклинг» — включает/отключает режим
- Поля рециклинга скрыты, пока не включен чекбокс:
  - `Порог рециклинга` — всё, что на этом уровне и ниже, идёт в рециклер
  - `ModuleRow` для рециклера (4 слота + редактор)
- Дополнительная строка в результатах: коэффициент рециклинга + шанс рециклера
- В таблице: строка «ПОТЕРИ (75% рециклинга)» и «ВСЕГО НА ВХОДЕ» с долей от входа

### Алгоритм (`calculate_with_recycling`)

Итеративный расчёт установившегося режима:

```
queue = {start_level: base_input}
final = {}
for each iteration:
    craft всё из queue через distribute() с Q_prod
    for each level lvl:
        if lvl <= recycle_threshold:
            recycle через distribute(lvl, Q_rec, amount * 0.25)
            результат → queue следующей итерации
        else:
            добавить к final
    если queue пуст — сходимость
```

**Ключевые свойства:**
- Каждый цикл рециклинга теряет 75% массы
- Система гарантированно сходится (убывающая геометрическая прогрессия)
- Качество модулей рециклера повышает качество возвращаемых материалов
- При `Q_rec = 0` возвращается 25% материала без изменения качества
- При `threshold = 1` рециклится только уровень 1 (Normal), остальные выходят

## 6. Тесты

45 тестов в `tests/test_calculator.py`:
- `TestQualityBonus` (6) — корректность бонусов, невалидные параметры
- `TestTotalChance` (7) — суммирование, кап на 100%, отрицательное кол-во, граничные случаи
- `TestTotalChanceFromModules` (5) — пустой список, смешанные слоты, none-слоты, эквивалентность старому API, слишком много слотов
- `TestDistribute` (10) — нулевой шанс, потолок, сумма долей = 100%, распределение, items_per_min == share * rate
- `TestRecycling` (10) — threshold, нулевой Q_rec, конвергенция, баланс массы, геометрическая прогрессия
- `TestRecyclingValidation` (7) — Q_prod>1, Q_rec<0, base_input<0, threshold вне диапазона, threshold==max_unlocked, сходимость

Запуск: `pytest tests/test_calculator.py -v`

## 7. Требования к окружению

- Python 3.10+
- PySide6 6.5+
- pytest 8.0+ (для тестов)
- Кроссплатформенность (Windows / Linux / macOS)
